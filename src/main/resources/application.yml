micronaut:
  application:
    name: todo-server
  server:
    cors:
      enabled: true
      configurations:
        web:
          allowed-origin-regex: "http://localhost(:\\d+)?"
          allowed-methods:
            - GET
            - POST
            - PUT
            - PATCH
            - DELETE
            - OPTIONS
          allow-credentials: true
          max-age: 3600